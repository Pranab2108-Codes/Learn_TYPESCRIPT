type User = {                 /* Same as interface structure of defining. */
                              /* Type can not be use to implement in a class like interface is doing. */
  firstName: string;
  lastName: string;
  age: number;

};