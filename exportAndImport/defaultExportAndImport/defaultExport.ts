const a = 1;


export default a;           /* While importing in another file, this "a" can be use different name. */
export const b = 2;         /* But in this type of exporting, means without the default we need to keep the name as same while importing this in another file. */